---
const projectsData = {
  title: "Portafolio de Proyectos",
  subtitle: "Soluciones desarrolladas con tecnologías modernas",

  projects: [
    {
      id: "landing-pages",
      title: "Landing Pages Modernas",
      category: "Frontend",
      description: "Desarrollo de páginas de aterrizaje responsivas y optimizadas para conversión, con diseños modernos y experiencia de usuario excepcional.",
      image: "/images/landing-preview.webp",
      technologies: ["Astro", "Tailwind CSS", "HTML5", "CSS3", "JavaScript"],
      features: [
        "Diseño responsive y móvil-first",
        "Optimización para SEO y performance",
        "Animaciones y transiciones fluidas",
        "Integración con formularios de contacto"
      ],
      status: "completed",
      year: "2025"
    },
    {
      id: "valorant-api",
      title: "Dashboard Valorant API",
      category: "Integración de APIs",
      description: "Aplicación web interactiva que consume la API de Valorant para mostrar estadísticas de jugadores, mapas y datos del juego en tiempo real.",
      image: "/images/valorant-preview.webp",
      technologies: ["JavaScript", "React", "API REST", "JSON", "CSS3"],
      features: [
        "Consumo de APIs externas en tiempo real",
        "Dashboard interactivo con gráficos",
        "Búsqueda y filtrado de jugadores",
        "Diseño responsivo y moderno"
      ],
      status: "completed",
      year: "2025"
    },
    {
      id: "inventory-system",
      title: "Sistema de Gestión de Inventario",
      category: "Full Stack",
      description: "Aplicación web completa para la gestión de inventarios con CRUD completo, sistema de autenticación y generación de reportes.",
      image: "/images/inventory-preview.webp",
      technologies: ["Python", "Flask", "MongoDB", "Bootstrap", "JWT"],
      features: [
        "CRUD completo de productos",
        "Sistema de autenticación seguro",
        "Reportes y analytics",
        "Panel de administración"
      ],
      status: "completed",
      year: "2024"
    },
    {
      id: "veterinary-system",
      title: "Sistema de Veterinaria",
      category: "Web App",
      description: "Plataforma web integral para gestión de citas veterinarias, historial médico de mascotas y administración de clientes.",
      image: "/images/vet-preview.webp",
      technologies: ["JavaScript", "Node.js", "MongoDB", "Express", "EJS"],
      features: [
        "Gestión de citas y calendario",
        "Historial médico de mascotas",
        "Sistema de clientes y mascotas",
        "Dashboard administrativo"
      ],
      status: "completed",
      year: "2024"
    }
  ]
};
---

<section id="proyectos" class="section-padding animate-on-scroll">
  <div class="container">
    <div class="projects-content">
      <!-- Header -->
      <div class="projects-header">
        <h2 class="section-title gradient-text">{projectsData.title}</h2>
        <p class="section-subtitle">{projectsData.subtitle}</p>
      </div>

      <!-- Projects Grid -->
      <div class="projects-grid card-animate">
        {projectsData.projects.map((project, index) => (
          <div class="project-card" data-category={project.category}>
            <div class="project-image">
              <div class="image-placeholder">
                <div class="project-icon">
                  {project.category === 'Frontend' && '🎨'}
                  {project.category === 'Integración de APIs' && '🔗'}
                  {project.category === 'Full Stack' && '⚡'}
                  {project.category === 'Web App' && '💻'}
                </div>
              </div>
              <div class="project-overlay">
                <div class="project-meta">
                  <span class="project-year">{project.year}</span>
                  <span class="project-status">{project.status}</span>
                </div>
              </div>
            </div>

            <div class="project-content">
              <div class="project-header">
                <div class="project-category">{project.category}</div>
                <h3 class="project-title">{project.title}</h3>
              </div>

              <p class="project-description">{project.description}</p>

              <div class="project-features">
                <h4 class="features-title">Características principales:</h4>
                <ul class="features-list">
                  {project.features.map((feature) => (
                    <li>{feature}</li>
                  ))}
                </ul>
              </div>

              <div class="project-tech">
                <h4 class="tech-title">Tecnologías:</h4>
                <div class="tech-stack">
                  {project.technologies.map((tech) => (
                    <span class="tech-badge">{tech}</span>
                  ))}
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- Call to Action -->
      <div class="projects-cta">
        <div class="cta-content">
          <h3 class="cta-title">¿Quieres ver más proyectos?</h3>
          <p class="cta-description">Explora mi repositorio completo en GitHub con más proyectos y contribuciones open source.</p>
          <a
            href="https://github.com/Deamacevedo"
            target="_blank"
            rel="noopener noreferrer"
            class="github-button"
          >
            <svg class="github-icon" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
            <span>Ver en GitHub</span>
            <svg class="arrow-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path d="M7 17l9.2-9.2M17 17V7H7" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
/* Section Styling */
#proyectos {
  background: linear-gradient(135deg, var(--primary-50) 0%, var(--accent-50) 100%);
  position: relative;
}

#proyectos::before {
  content: '';
  position: absolute;
  top: 0;
  right: -50%;
  width: 200%;
  height: 100%;
  background: linear-gradient(-45deg, transparent 40%, var(--primary-100) 50%, transparent 60%);
  opacity: 0.3;
  animation: shimmer-projects 6s ease-in-out infinite;
}

.projects-content {
  position: relative;
  z-index: 1;
}

/* Header */
.projects-header {
  text-align: center;
  margin-bottom: 4rem;
}

.section-title {
  font-size: clamp(2.5rem, 5vw, 4rem);
  font-weight: 800;
  margin-bottom: 1rem;
  letter-spacing: -0.02em;
}

.section-subtitle {
  font-size: 1.25rem;
  color: var(--neutral-600);
  font-weight: 500;
  margin: 0;
}

/* Projects Grid */
.projects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
  gap: 2rem;
  margin-bottom: 4rem;
}

.project-card {
  background: rgba(255, 255, 255, 0.95);
  border-radius: var(--radius-xl);
  border: 1px solid var(--primary-200);
  overflow: hidden;
  transition: all var(--transition-base);
  position: relative;
  cursor: pointer;
}

.project-card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-2xl);
  border-color: var(--primary-300);
}

/* Project Image */
.project-image {
  position: relative;
  height: 200px;
  background: linear-gradient(135deg, var(--primary-100), var(--accent-100));
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.image-placeholder {
  position: relative;
  z-index: 1;
}

.project-icon {
  font-size: 4rem;
  opacity: 0.7;
}

.project-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.1) 100%);
  display: flex;
  align-items: flex-end;
  padding: 1rem;
}

.project-meta {
  display: flex;
  gap: 0.5rem;
}

.project-year, .project-status {
  padding: 0.25rem 0.75rem;
  border-radius: var(--radius-md);
  font-size: 0.75rem;
  font-weight: 600;
  backdrop-filter: blur(10px);
}

.project-year {
  background: rgba(255, 255, 255, 0.9);
  color: var(--neutral-700);
}

.project-status {
  background: var(--accent-500);
  color: white;
}

/* Project Content */
.project-content {
  padding: 2rem;
}

.project-header {
  margin-bottom: 1rem;
}

.project-category {
  display: inline-block;
  padding: 0.25rem 0.75rem;
  background: var(--primary-100);
  color: var(--primary-700);
  border-radius: var(--radius-md);
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 0.75rem;
}

.project-title {
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--neutral-800);
  margin: 0;
  line-height: 1.3;
}

.project-description {
  font-size: 1rem;
  color: var(--neutral-600);
  line-height: 1.6;
  margin-bottom: 1.5rem;
}

/* Project Features */
.project-features {
  margin-bottom: 1.5rem;
}

.features-title {
  font-size: 1rem;
  font-weight: 600;
  color: var(--neutral-800);
  margin-bottom: 0.75rem;
}

.features-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.features-list li {
  display: flex;
  align-items: flex-start;
  gap: 0.5rem;
  font-size: 0.875rem;
  color: var(--neutral-600);
  line-height: 1.5;
}

.features-list li::before {
  content: '✨';
  flex-shrink: 0;
  margin-top: 0.125rem;
}

/* Project Tech */
.project-tech {
  border-top: 1px solid var(--neutral-200);
  padding-top: 1.5rem;
}

.tech-title {
  font-size: 1rem;
  font-weight: 600;
  color: var(--neutral-800);
  margin-bottom: 0.75rem;
}

.tech-stack {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.tech-badge {
  padding: 0.375rem 0.75rem;
  background: var(--accent-100);
  color: var(--accent-700);
  border-radius: var(--radius-md);
  font-size: 0.875rem;
  font-weight: 500;
  transition: all var(--transition-fast);
}

.tech-badge:hover {
  background: var(--accent-200);
  transform: translateY(-1px);
}

/* Call to Action */
.projects-cta {
  text-align: center;
  padding: 3rem 2rem;
  background: rgba(255, 255, 255, 0.8);
  border-radius: var(--radius-2xl);
  border: 1px solid var(--primary-200);
  backdrop-filter: blur(10px);
}

.cta-content {
  max-width: 600px;
  margin: 0 auto;
}

.cta-title {
  font-size: 2rem;
  font-weight: 700;
  color: var(--neutral-800);
  margin-bottom: 1rem;
}

.cta-description {
  font-size: 1.125rem;
  color: var(--neutral-600);
  line-height: 1.6;
  margin-bottom: 2rem;
}

.github-button {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1rem 2rem;
  background: linear-gradient(135deg, var(--neutral-800), var(--neutral-900));
  color: white;
  text-decoration: none;
  border-radius: var(--radius-lg);
  font-weight: 600;
  font-size: 1.125rem;
  transition: all var(--transition-base);
  box-shadow: var(--shadow-lg);
  position: relative;
  overflow: hidden;
}

.github-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
  transition: left 0.6s;
}

.github-button:hover::before {
  left: 100%;
}

.github-button:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-xl);
}

.github-icon {
  width: 1.5rem;
  height: 1.5rem;
}

.arrow-icon {
  width: 1.25rem;
  height: 1.25rem;
  transition: transform var(--transition-base);
}

.github-button:hover .arrow-icon {
  transform: translate(4px, -4px);
}

/* Animations */
@keyframes shimmer-projects {
  0%, 100% { transform: translateX(50%); }
  50% { transform: translateX(0%); }
}

/* Responsive Design */
@media (max-width: 768px) {
  .projects-grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .project-content {
    padding: 1.5rem;
  }

  .projects-cta {
    padding: 2rem 1rem;
  }

  .cta-title {
    font-size: 1.75rem;
  }
}
</style>